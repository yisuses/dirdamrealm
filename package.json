{
  "name": "@yisuses/whemotion",
  "version": "1.0.0",
  "description": "White Emotion Blog repository",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "sideEffects": false,
  "scripts": {
    "common": "yarn workspace @whe/common",
    "start:common": "yarn common start",
    "build:common": "yarn common build",
    "build:dep:common": "yarn build:common && mkdir -p dist && mv packages/common/dist dist/common",
    "build:storybook": "yarn common build:storybook",
    "test:common": "yarn common test",
    "release:common": "yarn --cwd packages/common run standard-version",
    "blog": "yarn workspace @whe/blog",
    "start:blog": "yarn blog dev",
    "build:blog": "yarn blog build",
    "build:dep:blog": "yarn build:blog && mkdir -p dist/blog && cp -r packages/blog/{.next,public,package.json,next.config.js,next-i18next.config.js} dist/blog",
    "test:blog": "yarn blog test",
    "release:blog": "yarn --cwd packages/blog run standard-version",
    "api": "yarn workspace @whe/api",
    "start:api:develop": "yarn api develop",
    "start:api": "yarn api start",
    "build:api": "yarn api build",
    "build:dep:api": "yarn build:api && mkdir -p dist && mv packages/api/build dist/api",
    "release:api": "yarn --cwd packages/api run standard-version",
    "releaseall": "run-s \"release:* {@}\" --",
    "release": "node scripts/release.mjs",
    "clean": "node scripts/clean.mjs",
    "build": "yarn clean && run-p 'build:dep:*'",
    "test": "yarn workspaces foreach -ptv run test --color",
    "test:watch": "jest --watch",
    "test:ci": "yarn test --silent --ci=true --coverage --coverageReporters=cobertura --coverageReporters=lcov",
    "lint": "eslint --fix packages/**/*.{ts,tsx}",
    "ncu": "ncu --deep",
    "prepare": "husky install",
    "postInstall": "patch-package"
  },
  "devDependencies": {
    "@commitlint/cli": "16.2.3",
    "@commitlint/config-conventional": "16.2.1",
    "husky": "7.0.4",
    "lint-staged": "12.4.0",
    "npm-check-updates": "12.5.9",
    "npm-run-all": "4.1.5",
    "patch-package": "6.4.7",
    "prettier": "2.6.2",
    "prompts": "2.4.2",
    "standard-version": "9.3.2",
    "ts-node": "10.7.0"
  },
  "engines": {
    "node": "^14.13.1 || >=16.0.0",
    "yarn": ">=1.22.0",
    "npm": "please-use-yarn"
  },
  "packageManager": "yarn@3.2.0"
}
